{
  "name": "{{projectName}}-server",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test 'src/**/*.test.js' '../shared/**/*.test.js'"
  },
  "dependencies": {
    "@loom/agent": "file:{{loomRoot}}/packages/agent",
    "@loom/server": "file:{{loomRoot}}/packages/server",
    "@loom/store": "file:{{loomRoot}}/packages/store",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "imports": {
    "#server/*": "./src/*",
    "#shared/*": "../shared/*"
  },
  "engines": {
    "node": ">=18"
  }
}
