---
name: scaffold-api
description: Scaffold server API routes and service with npm run scaffold:api -- Name
---

# Scaffold API

```bash
npm run scaffold:api -- Orders
```

Creates service, routes, test; patches `app.js`, `client.ts`, `loom.config.json`.
