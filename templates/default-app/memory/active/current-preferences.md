# Current preferences

Keep this active summary small. Detailed preference history goes to `memory/archive/preferences.jsonl`.

- Use Cursor MCP tools for research, not CLI installs, when MCP tools are available.
- Use `skills/optimized-research/` routing by default; user overrides always win.
- For external research, any valid method is allowed if more accurate, cheaper, or easier — state why in one line.
- Record model, skills, MCP tools, and depth choices on every run (`runMeta` on artifacts).
